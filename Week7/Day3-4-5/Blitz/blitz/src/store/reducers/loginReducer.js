const loginReducer = (state = {}, action) => {
    switch (action.type) {
        case "LOGIN_CORRECT":
            return state;
        default:
            return state;
    }
}
export default loginReducer
